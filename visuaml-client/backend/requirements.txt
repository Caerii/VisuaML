# Pinned for reproducible installs. Update deliberately when upgrading.
# If you get "No matching distribution found", your platform may only have newer
# builds—edit the torch/torchvision/torchaudio lines to match versions at
# https://pytorch.org/get-started/locally/
# Core PyTorch (use matching versions per release)
torch==2.9.1
torchvision==0.24.1
torchaudio==2.9.1

# Data processing
numpy==2.1.3
pandas==2.2.3

# Testing
pytest==8.3.4
pytest-cov==6.0.0

# Code quality
black==24.10.0
isort==5.13.2
flake8==7.1.1
mypy==1.13.0

# Optional: advanced model support (not imported by core API)
transformers==4.46.3
timm==1.0.24

# Optional: visualization debugging
matplotlib==3.9.2
seaborn==0.13.2

# Categorical/Hypergraph support
open-hypergraphs==0.1.2